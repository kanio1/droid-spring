apiVersion: v1
kind: Service
metadata:
  name: {{ include "bss-backend.fullname" . }}
  labels:
    {{- include "bss-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bss-backend.selectorLabels" . | nindent 4 }}
