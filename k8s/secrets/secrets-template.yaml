apiVersion: v1
kind: Secret
metadata:
  name: bss-secrets
  namespace: bss
  annotations:
    kubernetes.io/encryption: "true"
type: Opaque
data:
  # Base64 encoded values - replace with actual values
  # Use: echo -n 'value' | base64

  database-name: YnNz
  database-username:YnNzX2FwcA==
  database-password: <REPLACE_WITH_STRONG_PASSWORD>
  database-url: <REPLACE_WITH_JDBC_URL>

  keycloak-admin: YWRtaW4=
  keycloak-admin-password: <REPLACE_WITH_STRONG_PASSWORD>
  keycloak-issuer-uri: <REPLACE_WITH_KEYCLOAK_ISSUER_URI>
  keycloak-jwks-uri: <REPLACE_WITH_KEYCLOAK_JWKS_URI>

  grafana-admin-password: <REPLACE_WITH_GRAFANA_PASSWORD>
  acme-email: <REPLACE_WITH_EMAIL>

  # For production, use proper secrets management like:
  # - Kubernetes Secrets with encryption at rest
  # - HashiCorp Vault
  # - AWS Secrets Manager
  # - Azure Key Vault
  # - Google Secret Manager
